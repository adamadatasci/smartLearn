{
  "name": "interactivelearn",
  "version": "1.0.0",
  "private": true,
  "description": "InteractiveLearn — kids' learning games packaged as an iOS app.",
  "scripts": {
    "build": "node build.js",
    "ios:add": "npm run build && npx cap add ios",
    "ios:sync": "npm run build && npx cap sync ios",
    "ios:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
